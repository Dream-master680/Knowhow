# KnowHow 部署指南（阿里云轻量应用服务器）

> 适用场景：把前后端一体的 KnowHow 部署到阿里云轻量应用服务器，公网访问 + AI 可用。
>
> **部署形态**：整个项目由**一个 Node 进程**承载——Express 同时托管前端静态文件和 `/api/*` 接口，数据存 SQLite（`server/data.db`），AI 走服务端 DeepSeek 代理。所以服务器上只装 Node、跑一个进程即可。

---

## 0. 前提

- 一个 DeepSeek API Key（`server/.env` 里那个，已重建生效的 sk-b8c…）
- 一个阿里云账号
- 仓库地址：`https://github.com/Dream-master680/Knowhow.git`

---

## 1. 购买轻量应用服务器

1. 阿里云控制台 → **轻量应用服务器** → **创建实例**
2. 地区（**节点选择**决定要不要备案）：
   - **国内节点**（杭州/深圳等，便宜）：**绑定域名需要 ICP 备案**（耗时约 1–2 周）。
     不备案也能用：直接 `http://服务器IP:端口` 访问。**作业/演示推荐这条**。
   - **香港节点**（略贵、延迟稍高）：可绑域名且**免备案**。要正式域名再选这个。
3. 系统镜像：**Ubuntu 22.04**（或 24.04），2 核 2G 够用。
4. 创建后记下 **公网 IP** 和 **root 密码**。

---

## 2. 防火墙放行端口

轻量应用服务器控制台 → 你的实例 → **防火墙** 标签页 → **添加规则**：

| 端口 | 协议 | 说明 |
|---|---|---|
| 22 | TCP | SSH（默认已开） |
| 3000 | TCP | KnowHow 服务（下面用 3000 示范；若把端口改成 80 则放行 80） |

> 若打算 `PORT=80` 免端口号访问，就放行 **80** 并在第 6 步把 `PORT=80`。80 端口需要 root 运行（pm2 以 root 启动即可）。

---

## 3. SSH 登录服务器

Windows 自带终端或 PowerShell 直接：

```bash
ssh root@<公网IP>
```

（输 root 密码。也可以用 FinalShell / Xshell / VS Code Remote-SSH。）

---

## 4. 安装 Node.js 20

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs
node -v   # 确认 ≥ 18
```

> better-sqlite3 是原生模块，若 npm install 时没有对应预编译包会走源码编译，需要：
> `apt-get install -y python3 make g++`

---

## 5. 拉取代码 + 安装依赖

```bash
cd /opt
git clone https://github.com/Dream-master680/Knowhow.git knowhow
cd knowhow/server
npm install
```

---

## 6. 配置 `.env`

```bash
cp .env.example .env
nano .env
```

关键项（`.env` 已在 `.gitignore` 中，不会提交）：

```
PORT=3000                  # 想免端口号访问就改成 80
DEEPSEEK_API_KEY=sk-你新建的key   # 必填，否则 AI 不可用
```

> `config.js`（前端）里的 Key 已置空，前端不再携带任何密钥，安全。

---

## 7. 启动并保持运行（pm2）

```bash
npm install -g pm2
cd /opt/knowhow/server
pm2 start server.js --name knowhow
pm2 save
pm2 startup                # 按提示再执行它给出的一条命令，实现开机自启
```

常用管理命令：

```bash
pm2 logs knowhow      # 看日志
pm2 restart knowhow   # 重启
pm2 stop knowhow      # 停止
pm2 status            # 状态
```

---

## 8. 访问

- **不备案，用 IP**：
  `http://<公网IP>:3000`（记得第 2 步已放行 3000）
  默认账号：`admin/admin123`、`lawyer/123456`、`user/123456`
- **绑定域名**：
  - 香港节点：在域名 DNS 加一条 **A 记录 → 服务器公网 IP**，然后把 `PORT=80` 或反代到 80，即可 `http://你的域名` 访问
  - 国内节点：域名需先完成 **ICP 备案** 才能解析到国内服务器

---

## 9. 数据与备份

- 全部数据在 `server/data.db`（首次启动自动建表 + 种子账号）。
- **备份**：直接复制 `server/data.db` 文件即可（停机时复制最稳，运行中复制也可，SQLite 支持 WAL）。
- 生产建议：把默认测试账号 `admin/lawyer/user` 的密码改掉，或在管理后台停用。

---

## 10. 常见问题

| 现象 | 排查 |
|---|---|
| 公网打不开页面 | ① 防火墙/安全组没放行端口 ② 服务没起来（`pm2 status`）③ 80 端口被占用 |
| AI 报"未配置 Key" | `server/.env` 里 `DEEPSEEK_API_KEY` 为空 → 填好后 `pm2 restart knowhow` |
| AI 报 401 | Key 填错/被删 → 去 platform.deepseek.com 核对 |
| `npm install` 编译失败 | `apt-get install -y python3 make g++` 后重试 |
| 端口被占 | `ss -lntp` 看占用进程 |
