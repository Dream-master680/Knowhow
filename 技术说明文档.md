# KnowHow 技术说明文档（内部）

> 文档属性：内部技术版  
> 适用场景：开发、部署、运维、排查  
> 注：`README.md` 与 `项目说明文档.md` 为对外汇报版，刻意不包含本文工程细节。

---

## 一、总体架构

```
浏览器（现有前端，读写仍同步）                服务器（server/，Express + SQLite）
┌──────────────────────────────┐         ┌──────────────────────────────────────┐
│ index.html（静态）             │ 同源     │ server.js（Express）                  │
│ app.js / ai.js / settings.js  │ ─fetch─▶ │ · 托管前端静态文件（同源，无 CORS）     │
│ api.js（新增·同步桥 window.Sync）│         │ · /api/auth/*  登录/注册/改密          │
│ · 内存缓存镜像 localStorage     │         │ · /api/users/*  用户管理/审批          │
│ · 写→更新缓存+local+防抖同步    │ ◀─HTTP── │ · /api/bootstrap  拉全量              │
│ · 启动先推 dirty 再拉权威数据   │         │ · /api/sync       上送快照（LWW）      │
└──────────────────────────────┘         │ · /api/ai         DeepSeek 代理（SSE） │
                                        │ · SQLite（db.js → data.db）            │
                                        │ · .env（DeepSeek Key，gitignore 忽略）   │
                                        └──────────────────────────────────────┘
```

**核心思路 = 同步桥（sync-bridge）**：`app.js` 约 1 万行全是同步读 localStorage，逐一改异步风险高。`readStorage`/`writeStorage`（app.js:21-34）是全站全部读写调用的唯一汇聚点，改这两个函数体委托到 `window.Sync`，即覆盖全站所有读写。`Sync` 是透明缓存镜像：读写仍同步，后台与服务器同步。

### 服务端托管键（SERVER_KEYS，进 SQLite kv_rows）
`ln_forum_posts_v1`、`ln_community_feed_v1`、`ln_qa_items_v1`、`lawyer_cases`、`lawyer_clients`、`lawyer_appointments`、`lawyer_applications`、`legal_cases`、`legal_consultations`、`legal_messages`、`user_notifications`、`chat_sessions`、`chat_messages`、`user_friends`，以及按账号隔离的 AI 历史键 `ln_ai_(consult|widget)_history_<userId>`。

### 本地展示键（不进库）
`ln_films_v1`、`ln_news_v1`、`ln_lawyers_v1`、`ln_law_updates_v1`、`aboutInfo`、`ln_about_v2_done`、`ln_auth_v1`、`kh_*`。

### `users` 键特殊处理
用户数据**不走通用快照同步**（避免明文密码上送）。服务端 `safeUser` 投影永不返回 password/hash；`/api/sync` 处理 users 时丢弃 password 字段；登录/注册/管理全走专用 API。

---

## 二、两种运行模式

| 模式 | 启动方式 | 数据 |
|---|---|---|
| **后端模式（推荐）** | `cd server && npm install && npm start`，浏览器访问 `http://localhost:3000` | 数据持久化到 `server/data.db`（SQLite），换设备不丢 |
| **本地模式（双击 file://）** | 直接双击 `index.html` | 纯 localStorage（现状不变）；AI 咨询给友好提示「需通过后端访问」 |

后端模式下若服务器不可达，前端自动静默降级为纯 localStorage（`kh:syncfallback`），不阻断浏览。

**离线期间的写入**：写入先落 localStorage + 内存缓存（即时 + 离线可用），再 1500ms 防抖上送服务器；页面关闭时 `sendBeacon` 兜底推送。恢复联网后下一次加载先推 dirty 再拉权威数据。

---

## 三、启动与目录结构

```bash
cd server
npm install        # 首次
npm start          # 默认 3000 端口
```

```
server/
├── package.json        # 依赖：express / better-sqlite3 / dotenv（零其他）
├── .env                # PORT / DEEPSEEK_API_KEY / MODEL / BASE_URL / SESSION_TTL_MS（gitignore）
├── .env.example        # 提交用占位模板
├── server.js           # Express 入口：静态托管 + 挂载路由 + 非 /api GET 回退 index.html
├── db.js               # better-sqlite3：WAL + busy_timeout=5000；建表 + 首启种子
├── auth.js             # scrypt 哈希/校验、session token、authMiddleware、safeUser
├── seed-data.js        # 服务端种子（admin/lawyer/user + 论坛/社区/问答，与前端 seedIfEmpty 一致）
└── routes/
    ├── auth.js         # /api/auth/login|register|logout|change-password
    ├── users.js        # /api/users CRUD + 角色 + 密码重置 + 律师申请审批（admin 专用）
    ├── sync.js         # /api/bootstrap + /api/sync（optional-auth + LWW）
    └── ai.js           # /api/ai DeepSeek 代理（SSE 字节级透传 + 错误码透传）
```

---

## 四、SQLite 表

```sql
users   (id PK, username UNIQUE, password_hash, email, role, status, profile JSON,
         created_at, updated_at)
sessions(token PK, user_id FK→users ON DELETE CASCADE, created_at, expires_at)
kv_rows (key PK, data JSON, updated_at)              -- 键 = 前端 localStorage 键
ai_history(user_id, kind 'consult'|'widget', data, updated_at, PRIMARY KEY(user_id, kind))
```

- 角色：`superadmin`(admin 账号) / `admin` / `lawyer` / `lawyer_pending` / `user`
- 律师注册 → `role=lawyer_pending` + `status=pending`，并写入 `kv_rows.lawyer_applications`，由管理员审批
- 密码哈希：Node 内置 `crypto.scrypt`，格式 `$salt$hash`（零额外原生依赖）

---

## 五、同步桥机制要点（api.js）

1. **写透**：`write` 同步更新内存缓存 + localStorage（保离线行为不变）→ 服务端键标 dirty。
2. **防抖**：1500ms 后 `POST /api/sync` 上送 `{ keys: { [key]: { data, updatedAt } } }`；`beforeunload` 用 `sendBeacon` 兜底（sendBeacon 不带 token，关页私有写入会被当游客拒 → dirty 保留，下次加载带 token 重推，不丢仅延迟）。
3. **bootstrap（启动流程）**：先推 dirty（恢复离线写入）→ 再 `GET /api/bootstrap`（**带 token**）拉权威数据覆盖缓存 + localStorage → **只清「被接受」的 dirty 键**（被拒的保留，登录后重推，不丢未上送数据）→ 派发 `kh:datasync`（app 重渲当前页拿服务器数据）。登录 / 登出 / 401 后自动重新 bootstrap（`scheduleReload`，防重入）。
4. **合并写（防并发丢失）**：数组键服务端按 **id 合并**（并集 + item 级新鲜度 `updatedAt||createdAt||appliedAt||lastReadAt||0` + 墓碑），**不再整键覆盖** → 两个用户同一窗口各写一条，后写不再覆盖先写；`aboutInfo`（对象）保留整键 LWW。
5. **墓碑删除**：客户端删除 = 给 item 打 `_deleted:true` + 刷新 `updatedAt` 再整数组回写；服务端存储保留墓碑、**所有响应与公开 `Sync.read` 剥离墓碑** → 渲染层零改动；他人旧快照/再 flush 不复活。
6. **401 统一处理**：任一 API 返回 401 → 清 `ln_auth_v1` + 派发 `kh:authchange` + 以游客身份重新 bootstrap（**不降级本地模式**，坏 token 下 bootstrap 由服务端按游客返回过滤数据）。
7. **降级**：bootstrap 网络失败 → `localMode=true` → 派发 `kh:syncfallback`，app 补播被跳过的服务端键，纯本地可用。

---

## 六、认证与安全

| 项 | 说明 |
|---|---|
| 密码存储 | 服务端 scrypt 哈希；本地 users 不再存明文（后端模式下） |
| 会话 | `crypto.randomBytes(32)` token，30 天有效（`.env SESSION_TTL_MS`），可吊销 |
| 认证中间件 | optional-auth：带有效 token 挂 `req.user`；`required:true` 时无 token 返回 401 |
| safeUser | 服务端永远只返回 `{id, username, email, role, status, profile, createdAt, updatedAt}`，**无 password/hash** |
| 改密 | 校验当前密码 → 更新哈希 → **吊销该用户全部 session**（前端登出回首页） |
| admin 角色保护 | admin 账号必须保持 superadmin；他人不可被设为 superadmin；不可删除 admin |
| 密码不回填 | 在线模式下编辑用户表单密码留空（重置密码功能），用户名只读 |
| 律师审批 | 通过→`role=lawyer/active`；拒绝→`status=rejected` |

**按用户读写隔离（部署前加固，server/db.js `readOwner`/`writeOwner`）**：
- **读**：bootstrap/poll 按登录用户过滤私有键。admin 全量；游客只见公开键、私有键全空、`users` 空；普通用户只见归属自己的内容（聊天双方互见、法律咨询/案件 `userId`+`lawyerId`、通知 `toUserId/userId`、好友 `userId/lawyerId`、工作台 `lawyerId`、`lawyer_applications` 仅 admin）。`users` 对登录用户保持全量安全投影（消息/好友选接收人需要 4 个消费点）。
- **写（/api/sync 分层鉴权）**：游客只能 **seed 空公开键**（空服务器首访自举）、已有内容公开键一律拒、私有键一律拒；登录用户私有键按归属过滤（如 `chat_messages` 只保留 `senderId===本人`，伪造他人身份的消息被服务端丢弃）；`lawyer_applications` 仅 admin 可写；`users` 分支仅 admin 可写。
- **工作台归属补全**：`lawyer_cases/clients/appointments` 创建时补 `lawyerId`（否则非 admin 看不到）。
- 返回结构：sync 响应 `{ ok, accepted, rejected }`，`rejected` 为未接受键名数组，前端保留其 dirty 待登录后重推。

**DeepSeek Key**：真实 Key 只在 `server/.env`，前端 `config.js` 已置空。`/api/ai` 服务端注入 `Authorization`，错误码（401/402/429/5xx）原样透传，前端 `ai.js` 现有文案映射自动生效。

> ⚠️ **待办**：旧的 DeepSeek Key（`sk-6ffe...`）已随公开仓库泄露，请到 platform.deepseek.com **吊销旧 Key** 并新建，填入 `server/.env`。代码侧已把 Key 移出浏览器。

---

## 七、API 一览

| 路由 | 方法 | 认证 | 说明 |
|---|---|---|---|
| `/api/auth/login` | POST | 公开 | scrypt 校验 → `{user, token}` |
| `/api/auth/register` | POST | 公开 | 查重 → 入库；律师注册返回 `pending` |
| `/api/auth/logout` | POST | 可选 | 吊销当前 token |
| `/api/auth/change-password` | POST | 登录 | 校验当前密码 → 更新 → 吊销全部 session |
| `/api/bootstrap` | GET | **可选** | 全部托管键（**按登录用户过滤私有键**）+ users（登录用户全量安全投影 / 游客空）+ 登录附带本账号 ai_history |
| `/api/sync` | POST | **可选** | 上送各键快照；**数组键按 id 合并（并集+新鲜度+墓碑），aboutInfo 整键 LWW**；写入按用户鉴权（游客只 seed 空公开键）；返回 `{ok, accepted, rejected}` |
| `/api/users` | POST | admin | 新增用户 |
| `/api/users/:id` | PUT/DELETE | admin | 改 email/role/status / 删用户 |
| `/api/users/:id/role` | POST | admin | 切换角色（superadmin 规则服务端强制） |
| `/api/users/:id/password` | POST | admin | 重置密码 + 吊销该用户全部 session |
| `/api/users/:id/profile` | POST | 本人 | 保存资料 |
| `/api/users/lawyer-applications/:id/approve` | POST | admin | 律师审批（同时更新用户角色） |
| `/api/users/lawyer-applications/:id/reject` | POST | admin | 同上（拒绝） |
| `/api/ai` | POST | 公开 | DeepSeek 代理，SSE / JSON 透传 |

---

## 八、阿里云部署

1. 装 Node 20 LTS：
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
   sudo apt-get install -y nodejs build-essential python3   # better-sqlite3 有预编译产物
   ```
2. `git clone` 项目 → `cd server && npm install`。
3. `cp .env.example .env` 填新 Key（旧 Key 在 DeepSeek 后台吊销）。
4. pm2 托管（**单实例 fork 模式**，勿开 cluster，避免多进程抢 SQLite 写）：
   ```bash
   pm2 start server.js --name knowhow && pm2 save
   ```
5. 防火墙放行端口；Nginx 反代 + `proxy_buffering off`（SSE 必须）：
   ```nginx
   location / {
     proxy_pass http://127.0.0.1:3000;
     proxy_http_version 1.1;
     proxy_buffering off;
     proxy_cache off;
     proxy_read_timeout 300s;
   }
   ```
   hash 路由无需 rewrite（所有路由在 `#` 后，服务端只收到 `/`）。
6. `.gitignore` 已含 `.env`、`node_modules/`、`server/data.db*`。

---

## 九、已知限制与后续（v2 方向）

| 限制 | 现状（v1） | v2 方向 |
|---|---|---|
| ~~多用户并发写~~ | 数组键已按 id 合并（并集+新鲜度+墓碑），并发写不丢；残留「同一 item 平局 client-wins」窄窗口 | — |
| ~~私密数据全量 bootstrap~~ | 已按登录用户过滤 chat/messages/notifications 等私有键，游客只见公开键 | — |
| AI 滥用烧 Key | `/api/ai` 公开 + 校验消息条数/长度 | 按 IP 限流 |
| better-sqlite3 编译失败 | 用 Node 20 LTS（prebuild） | Node 22.5+ 内置 `node:sqlite` / `sql.js` |
| file:// 无后端 | 纯本地能力全量可用，AI 给友好提示 | — |

---

## 十、本地验证清单（开发用）

- 起服务后访问 `http://localhost:3000`，登录 `admin/admin123`（或 `lawyer/123456`、`user/123456`）。
- 双标签页：A 改论坛数据刷新 B → 内容一致（bootstrap 拉取）。
- 改服务端 `kv_rows` → 刷新前端可见。
- 断网写入 → 恢复后自动推送（dirty 持久化在 `kh_sync_dirty`）。
- AI 咨询：`.env` 填真实 Key 后流式对话；`file://` 双击打开给出后端提示。
- 多账号并发/隔离（部署前加固回归，临时脚本已删）：并发两条消息均在（不丢）；游客/第三方看不到他人私有数据；游客伪造消息/覆盖已有内容被拒、空键 seed 收；伪造 `senderId` 被丢弃；删除后他人旧快照 flush 不复活；坏 token 回退游客视图。
